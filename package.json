{
  "name": "koco-tabs",
  "version": "1.2.0",
  "homepage": "https://github.com/cbcrc/koco-tabs",
  "authors": [
    "fcharest <frederik.charest@radio-canada.ca>"
  ],
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "keywords": [
    "tabs",
    "bootstrap",
    "koco"
  ],
  "license": "MIT",
  "dependencies": {
    "bootstrap": "~3.3.6",
    "jquery": "~2.2.4",
    "knockout": "~3.4.0",
    "koco": "^1.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
